package com.flightbookingapp.model;

/**
 * Operational states for a scheduled flight.
 */
public enum FlightStatus {
    SCHEDULED,
    BOARDING,
    DEPARTED,
    ARRIVED,
    CANCELLED,
    DELAYED
}
